# Database
# Local (fora do Docker):
DATABASE_URL="postgresql://username:password@localhost:5432/siggflow"
# Docker (dentro do compose, use host "postgres"):
# DATABASE_URL="postgresql://siggflow:siggflow@postgres:5432/siggflow"
 
# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here" 

# SMTP para envio de emails de redefinição de senha
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=eduardodevtech@gmail.com
SMTP_PASS=sua_senha_de_app 